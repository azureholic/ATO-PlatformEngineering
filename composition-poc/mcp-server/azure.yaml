# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: catalog-mcp-server
metadata:
  template: catalog-mcp-server@1.0.0

services:
  api:
    project: ./src
    language: dotnet
    host: containerapp
    docker:
      path: Dockerfile
      context: .
